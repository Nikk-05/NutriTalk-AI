import {createSlice, configureSlice} from '@reduxjs/toolkit'

const initialState = {
    name: ""
}
const UserSlice = createSlice({
    name:'user',
    initialState:initialState,
    reducers:{
        
    }
})